from setuptools import find_packages, setup

setup(
    name="voyager",
    version="0.1",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[
        # 여기에 필요한 다른 패키지를 나열할 수 있습니다.
    ],
)
